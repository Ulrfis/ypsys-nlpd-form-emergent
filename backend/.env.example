# CORS: origins allowed to call the API (comma-separated). Default: http://localhost:3000, http://127.0.0.1:3000
# CORS_ORIGINS=http://localhost:3000,https://your-app.vercel.app

# OpenAI (for /api/analyze proxy - avoids CORS and exposes key only server-side)
OPENAI_API_KEY=your-openai-api-key
OPENAI_ASSISTANT_ID=your-openai-assistant-id

# Admin API (optionnel) : clé secrète pour GET /api/submissions et /api/stats.
# - Si vous NE L'UTILISEZ PAS (vous lisez les données uniquement dans Supabase) : ne rien définir.
#   Les routes restent refusées (403), c'est voulu.
# - Si vous avez un back-office ou un script qui appelle ces routes : définir une valeur
#   secrète ici, et envoyer l'en-tête X-API-Key: <cette même valeur> dans vos requêtes.
# API_ADMIN_SECRET=your-secret-key
