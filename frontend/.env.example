# =============================================================================
# BACKEND (obligatoire pour l’analyse OpenAI)
# =============================================================================
# URL du backend. L'appel OpenAI passe par ce backend (clé API côté serveur uniquement).
#
# En LOCAL :       http://localhost:8000
# En PRODUCTION :  l'URL de votre backend déployé (ex. Railway)
#                  Exemple : https://votre-projet-backend.railway.app
#
# Lors du déploiement du frontend (Railway/Vercel), définir cette variable dans
# les paramètres d'environnement du build avec l'URL réelle du backend.
REACT_APP_BACKEND_URL=http://localhost:8000

# =============================================================================
# SUPABASE – Base de données (sauvegarde des soumissions du formulaire)
# =============================================================================
# À mettre UNIQUEMENT ici (frontend). Ne pas mettre les clés Supabase dans le backend.
#
# Où les trouver :
#   1. Ouvrez votre projet sur https://supabase.com/dashboard
#   2. Settings (icône engrenage) → API
#   3. Project URL → copiez dans REACT_APP_SUPABASE_URL
#   4. Project API keys → anon public → copiez dans REACT_APP_SUPABASE_ANON_KEY
#
REACT_APP_SUPABASE_URL=https://VOTRE_PROJECT_REF.supabase.co
REACT_APP_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# NE PAS METTRE de clés OpenAI ici (REACT_APP_* = inclus dans le bundle = exposé).
# =============================================================================
# Utilisé seulement si REACT_APP_BACKEND_URL n’est pas défini (appel direct, déconseillé).
# Mettre OPENAI_API_KEY et OPENAI_ASSISTANT_ID uniquement dans backend/.env
